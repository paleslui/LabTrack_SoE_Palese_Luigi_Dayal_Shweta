# LabTrack — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
Flask>=3.0,<4.0

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy>=2.0,<3.0       # ORM layer — swap SQLite for PostgreSQL via DATABASE_URI

# ── Authentication ────────────────────────────────────────────────────────────
bcrypt>=4.0,<5.0           # Password hashing (NFR-02)

# ── Background jobs / email ───────────────────────────────────────────────────
Flask-APScheduler>=1.13,<2.0   # Scheduled expiry-check job for email alerts

# ── QR code labels ────────────────────────────────────────────────────────────
qrcode[pil]>=8.0,<9.0          # QR code generation for printable sample labels

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=9.0,<10.0
pytest-cov>=5.0            # Coverage reporting

# Security
python-dotenv>=1.0,<2.0   # Load SECRET_KEY and credentials from .env
flask-limiter>=3.0,<4.0   # Rate limiting — prevents brute force attacks
